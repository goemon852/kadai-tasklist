<h1>新しいタスクの追加ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, "タスク" %>
    <%= f.text_field :content  %>
    
    <%= f.submit "リストに追加" %>
<% end %>

<%= link_to "一覧へ戻る", tasks_path %>